[package]
name = "dependency-check-updates-github"
version = "0.2.0"
description = "GitHub Actions workflow support for dependency-check-updates"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
documentation.workspace = true
readme.workspace = true
keywords = ["dependency", "update", "github-actions", "workflow", "ncu"]
categories = ["development-tools"]

[lib]
name = "dependency_check_updates_github"
path = "src/lib.rs"

[dependencies]
dependency-check-updates-core.workspace = true
# Workflows spell their job / service containers with the same `image:` key a
# Compose file uses, so the container scanner is reused rather than duplicated.
dependency-check-updates-docker.workspace = true
reqwest.workspace = true
serde.workspace = true
tokio = { workspace = true, features = ["sync"] }
node-semver.workspace = true
tracing.workspace = true
futures.workspace = true

[dev-dependencies]
rstest.workspace = true
serde_json.workspace = true
tokio = { workspace = true, features = ["rt-multi-thread"] }
wiremock.workspace = true
rustls.workspace = true

[lints]
workspace = true
